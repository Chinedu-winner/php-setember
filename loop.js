let courses = ["php", "HTMI", "CSS"]

console.log(course[1]);

for (let index = 0; index < array.length; index++) {
    const element = array[index];
    console.log(`My best course is ${element}`);
    
}